# Este archivo es utilizado automáticamente por Docker Compose para desarrollo local.
# Agrega los mounts de volumen para habilitar recarga en caliente (hot-reloading).

services:
  backend:
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules
